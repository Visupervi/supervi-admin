import { request } from "./request"
import { rotuerViews } from "./routeHandle"
import { getToken, setToken, clearToken } from "./token"
export {
  request,
  rotuerViews,
  getToken,
  setToken,
  clearToken
}